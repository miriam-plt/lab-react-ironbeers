import { NavLink } from 'react-router-dom';

function Navbar() {
        return(
            <NavLink to="/">
                <header>Homepage</header>
            </NavLink>
        )
    
}

export default Navbar;